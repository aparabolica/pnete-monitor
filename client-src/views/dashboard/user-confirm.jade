section#confirmation.mastform
  h1 Confirmação de email
  .main
    p Para confirmar sua conta defina uma senha abaixo.
  form(ng-submit="confirm(user)")
    p
      label(for="pwd_input") Digite uma senha
      input#pwd_input(type="password" ng-model="user.pwd")
    p
      label(for="pwd_repeat_input") Confirme a senha
      input#pwd_repeat_input(type="password" ng-model="user.pwd_repeat")
    p
      input(type="submit" value="Entrar")
.message-notification(ng-controller="MessageCtrl")
  .message-item(ng-repeat="message in messages" ng-show="message.text")
    .message-content
      a.close-button(ng-click="close(message)" href="javascript:void(0);") x
      p(ng-class="message.status" ng-bind-html="getMessage(message)")
